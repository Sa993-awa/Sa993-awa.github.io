
---
layout: post
title: Design Pattern II - Structural Pattern
subtitle: Organising Classes Effectively
categories: Design Patterns
tags: [Adapter,Bridge,Composite,Decorator,Facade,Proxy,Flyweight]
---

📝
 
### Initial Post

## Adapter Pattern 

 Consider an online store that uses a modern e-commerce platform to manage customer orders and process payments. In this Python example, the modern e-commerce platform represents the "modern system," while the legacy payment system represents the "old system" .The "payment adapter" works  as a bridge connecting them. In addition, this feature allows the modern system to operate without needing to understand  or modify the legacy system's existing code.

 (Sarcar, 2022) explains that, the Adapter pattern enables us to integrate multiple classes, modules, or functions with differing designs and interfaces without needing to modify them; the structure of the adapter is determined by the existing modules and classes we aim to connect.


```Python```

from abc import ABC, abstractmethod
import json


class PaymentProcessor(ABC):
    @abstractmethod
    def pay(self,payment_data):
        pass 

class LegacyPaymentSystem:
    def setup_payment(self,amount_in_GBP):                  #Old SOAP-based API
        print(f"Legacy system processed payment of {amount_in_GBP}")

#create the Adapter 

class paymentAdapter(PaymentProcessor):  

    def __init__(self,legacy_system):
        self.legacy_system =legacy_system

    def pay(self,payment_data):

        payment=json.loads(payment_data)
        self.legacy_system.setup_payment(payment["amount"])



class EcommercePlatform:                               #Modern E-commerce platform
                                                              # expecting RESTful JSON APIs
    def __init__ (self,Payment_Processor):    
        self.payment_processor=Payment_Processor

    def  checkout(self,amount):

        print("processing payment!!!")  
        payment_data=json.dumps({
            "amount":amount,
            "currency":"GBP"
        })

        self.payment_processor.pay(payment_data)

if __name__ == "__main__":
      
      legacy_payment=LegacyPaymentSystem()

      payment_adapter= paymentAdapter(legacy_payment)  

      Ecommerce = EcommercePlatform(payment_adapter)   #connect the Adapter to the Modern e_commerce platform

      Ecommerce.checkout(50)

      ```

## Bridge Pattern 
 The company sells various of electronic devices, such as TVs and radios, and also offers numerous types of remote controls, including both basic and advanced models.

 without the Bridge pattern, we need to build a code with different classes such as BasicTVRemote, AdvancedTVRemote , BasicRadioRemote and AdvancedRadioRemote , which mean that in this case the number of classes would increase quickly as more devices and remote types were added.

 The Bridge pattern decouples the problem into abstraction and implementation components.
 In my code example ,the abstract class  (ElectronicDevice ) defines that each electronic device  must have turn_on and turn_off function.
In addition, we can add extra functionality such as timer through the AdvancedRemoteControl.

      ```Python```
from abc import ABC, abstractmethod

#Abstract base class defining the common interface for electronic devices 

class ElectronicDevice (ABC):
    @abstractmethod
    def turn_on(self):
        pass

    @abstractmethod
    def turn_off(self):
        pass

#Implementation 
class TV(ElectronicDevice):
    

    def turn_on (self):
        print("TV is  now ON")


    def  turn_off(self):
        print("TV is now OFF") 


class Radio(ElectronicDevice):
   
    def  turn_on(self):
        print("Radio is now ON")  

    def turn_off(self):
       print("Radio is now OFF")

#Remot control that can work with any ElectronicDevice

class RemoteControl:                   
    def __init__ (self,device:ElectronicDevice ):   
       self.device =device   

    def turn_on(self):
        self.device.turn_on()

    def  turn_off(self):
        self.device.turn_off() 


class AdvancedRemoteControl(RemoteControl):    # Ability to add extra function
      
      def stop_timer(self,hours):
         print(f"Dvice will turn off within {hours}hours")


if __name__=="__main__":
             
     tv = TV()
     basic_remote = RemoteControl(tv)

     basic_remote.turn_on()
     basic_remote.turn_off()

     print()

    
     radio=Radio()
     basic_radio_remote = RemoteControl(radio)
     basic_radio_remote.turn_on()
     basic_radio_remote.turn_off()

     print()


     advanced_romte=AdvancedRemoteControl(tv)
     advanced_romte.turn_on()
     advanced_romte.stop_timer(3)
     advanced_romte.turn_off()

     ```
## Composite Pattern
For my scenario, I considered an infant school which has a year group contains different classes. I gave each class a different colour to distinguish it. I will apply Composite pattern which Enables the arrangement of objects into tree structures to represent part-whole hierarchies. The Composite pattern allows clients to treat individual objects and groups of objects uniformly.(Sarcar, 2022)
The Composite Pattern will organise the school into a hierarchical structure. The School contains YearGroup objects, while each YearGroup contains Class objects. 

 I created a( SchoolComponent) which provides a common interface through the show() method, this will allow the different components to be treated in a similar way. This makes the system easier to extend because we can add a new year groups or classes without changing the existing classes.

    ```Python```
from abc import ABC, abstractmethod
#commen interface for all school components
class SchoolComponent(ABC):
    @abstractmethod
    def show(self):
        pass


#represent an individual class
class Class(SchoolComponent):
    def __init__(self,name,colour):
        self.name=name
        self.colour=colour


    def show(self):
        print(f"Class:{self.name} - colour:{self.colour}")


#represent a year group
class YearGroup(SchoolComponent):      #common interface

    def __init__(self,year):
        self.year=year
        self.children=[]

    def add(self,component):
        self.children.append(component)

    def show(self):
        print(f"{self.year}")

        for child in self.children:
            child.show()

class School(SchoolComponent):
    def __init__(self,name):
         self.name=name
         self.children=[]

    def add(self,component):
        self.children.append(component)  

    def show(self) :
        print(f"School:{self.name}")  

        for child in self.children:
            child.show()

#create the school
school = School("infant school")          

#create year groups

Year1=YearGroup("Year 1")
Year2=YearGroup("Year 2")

#create classes with different colours
blue_class=Class("Blue Class","Blue")
green_class=Class("Green class","Green")


red_class=Class("Red Class","Red")
orange_class=Class("Orange Class","Orange")


Year1.add(blue_class)
Year1.add( green_class)

Year2.add(red_class)
Year2.add(orange_class)


school.add(Year1)
school.add(Year2)

school.show()

````
*Sarcar, V. (2022) Java design patterns: a hands-on experience with real-world examples. New York: Apress. Available at:      https://learning.oreilly.com/library/view/java-design-patterns/9781484279717/html/395506_3_En_4_Chapter.xhtml (Accessed:     15 August 2026).*

*Sarcar, V. (2022) Java design patterns: a hands-on experience with real-world examples. New York: Apress. Available at:     https://learning.oreilly.com/library/view/java-design-patterns/9781484279717/html/395506_3_En_4_Chapter.xhtml (Accessed: 15  August 2026).*


📒 🖊️
## Peer Disussion

#### Response to *Molly Fraser*

 I enjoyed reading your post; your explanation of the three design patterns was clear and easy to understand.
The examples you provided effectively illustrate the purpose of each pattern and its importance in practical software development.

I particularly liked the example regarding the "Adapter Pattern".
Converting a payment amount into "pence" before sending it to a legacy SOAP system was an excellent illustration of how the adapter's role goes beyond merely connecting two different interfaces to actually transforming data into the format expected by the other system. I think the example would appear even more realistic if the legacy system returned an XML response that the adapter then converted to JSON to suit the modern system, as you mentioned in your explanation.

 I also found your example of the "Bridge Pattern" useful. It clearly demonstrated how relying on inheritance can lead to the creation of numerous distinct classes, such as (BasicTVRemote), (AdvancedTVRemote), and (BasicRadioRemote).

The example regarding the "Composite Pattern" was also helpful; the size() function clearly illustrates how individual files and groups of files can be handled in the same way.



#### Response to *Kaona Hoxha*

Thanks for sharing your ideas.

I found your examples interesting. The payment system is clear example and your design in the Adapter separates between the legacy system and the checkout system, this clarifies if the old system changes, we can make changes without modifying the main application.
In addition, I like your Bridge Pattern example. I think separating the remote from the device makes the code easier to extend if we want to add or create a new device. But I am wondering if we need to have both a basic remote and an advanced remote, how can we extend the remote and add extra class? could the advanced remote inherit from RemoteControl and add extra functions such as a timer?

Your composite example structured very well , the way Folder.show() loops through self.item and call .show() on each one is a clean way to demonstrate recursion without overcomplicating it. Also, your scenario is excellent real-world example and suits your code.
