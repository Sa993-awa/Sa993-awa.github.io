---
layout: post
title: Unit 7 Secure Coding Practices in Object-Oriented Programming
subtitle: Building Secure Software
categories: Security
tags: [Secure-Coding, OOP]
---

### 🖇️Collaborative Discussion 3   


## 📝 Initial Post   

🟢 The provided Python authentication system had several security vulnerabilities that put users accounts at risk. 
The first issue was that passwords were stored in plaintext. Passwords such as "admin123" and "password" were stored directly, which means that if an attacker gained access to the database, they could read  every password easily. A safer way is to use bcrypt or Argon2 to hash passwords. A salt is also used with password during hashing to make the stored passwords harder to crack.


The second problem is the potential vulnerability to SQL injection attacks. Malicious inputs such as ,`'admin' OR '1'='1'` _even though it does not use a database_ could create a risk in systems that construct SQL queries based on user input. 


In addition, strong password requirements should be introduced, since a poorly chosen password allows the attacker access it directly. And there are some rules for perform a strong password which are addressed by Baker (2022) :

"Password has a minimum length (e.g., nine characters).
"Password contains lowercase letters, uppercase letters, and symbols".
 
 Also, there was no input validation, meaning empty strings or malicious payloads could be accepted.

Finally, rate limiting should be added to restrict repeated failed attempts, avoiding unlimited login attempts. Which allows attackers to carry out brute-force attacks multiple times until they successfully gain accessing.


Refactoring steps : 

Hash password : use bcrypt with a salt to hash the passwords before storing them. 

Secure Authentication : use bcrypt.checkpw to compare the entered password against the stored hash, instead of comparing plain strings. 

Encapsulation: restrict access to sensitive data using private and protected modifiers.

Enforce Password Policies: enforces strong password including minimum length ,lowercase letter, uppercase letter and symbol. 


Add Rate Limiting: reduced the risk of brute-force by limiting the  number of repeated failed login attempts.

``` Python

import bcrypt

class User:
    def __init__ (self,username, password):
        self._username=username
        self._password= bcrypt.hashpw(password.encode(),bcrypt.gensalt())   # Hash and salt the password befor storing it


class AuthenticationSystem:
    def __init__(self):
        self._users=[]
        self._failed_attempts = {}

    def add_user(self,username,password):

        if not username or not password:
            print("Username and password cannot be empty.")
            return False
#check that the password contains lowercase,uppercase and symbol
        if len(password) < 10:
            print("Password must be at least 10 characters.")
            return False
                
        has_lowercase=any(char.islower()for char in password)
        has_uppercase=any(char.isupper()for char in password)
        has_symbol=any(not char.isalnum()for char in password)

        if not(has_lowercase and has_uppercase and has_symbol):
          print( "password must contain lowercase,suppercase and symbol.")
          return False


   #Store the user after the password has been securely hashed
        self._users.append(User(username,password))
        self._failed_attempts[username] = 0

        return True
  
    def authenticate(self,username,password):

        attempts = self._failed_attempts.get(username,0)                     # Adding limit attempts

        if attempts >= 4:           # Limit the number of login attempts to prevent brute-force attacks
            return False

        for user in self._users:
            if user._username==username:

                if bcrypt.checkpw(password.encode(),user._password):
                    self._failed_attempts[username]=0
                    return True
                
                else:         # Compare the entered password with the stored bcrypt hash
                    self._failed_attempts[username] = attempts + 1
                    return False
                
                return False
                

auth_system = AuthenticationSystem()

auth_system.add_user("Sally", "Sally1993@")      # strong password
auth_system.add_user("Sally", "saly1993")        # rejected: less than 10 char, no uppercase and symbol

print(auth_system.authenticate("Sally","sally1993@"))      #First try
print(auth_system.authenticate("Sally","sally1993"))      #Second try

print(auth_system.authenticate("Sally","Sally1993@"))     #Test successful login#
```

## 📖 References:

*Baker, M. (2022) Secure Web Application Development: A Hands-On Guide with Python and Django. 1st edn. Berkeley, CA: Apress.*

*(UoEO (University of Essex Online) (n.d.) Secure Coding Techniques [Lecturecast transcript]. Advanced Object-Oriented Design and Programming, Unit 7: Secure Coding Practices in Object-Oriented Programming.*

### 📒🖊️ Peer Discussion

#### My comment on *Séba`s Daher* post

🔸Thanks for sharing your ideas.

I really like the way you structured this task. You addressed each point clearly, which makes it easy to follow and understand the vulnerabilities before refactoring the code.

Also I agree with your point about the SQL injection example being misleading, because the original code does not actually connect to a database.

One part I particularly enjoyed in your code was using time for the lockout. In my own code , I added a limit to the number of failed attempts , but I did not include a time limit.
Your approach is better because the account is only locked temporarily, so that makes the user to try again after the lockout period. This also helps protect against brute force attacks.

overall, I think your code and your explanation addressed precisely and the security improvement are obviously connected to the vulnerabilities you identified.

#### ⬛ PEER FEEDBACK:

#### *Séba`s Daher* comment on my post:

⚫ Sali, I tested registering "NewUser" twice with a different valid password. It doesn't overwrite the original password the way the issue Reem flagged on my own code did, instead a second account gets silently created with the same username, and only the first one is ever checked when logging in, so the second account's own password never actually authenticates.

Retrying it repeatedly increments the same failed-attempt counter though, and once that reaches the limit, testing confirmed something critical: the original, legitimate "NewUser" account gets locked out too, even with its correct password, since attempts are tracked per username string rather than per individual account. So the missing uniqueness check doesn't just create a dead duplicate, it opens a way to lock a real user out of their own account.

On the password check, complexity is covered but nothing stops password from equaling username outright. Testing add_user("BobSmith1993@", "BobSmith1993@") confirmed it: the call passed every check and the account authenticated successfully afterwards. Baker (2022) lists this exact rule, that a password should not resemble the username, alongside the length and composition rules already implemented here, so it would sit naturally in the same validation block.

Worth flagging, this check isn't in my own code either, the idea came from reviewing peers' code rather than something I'd already built in.

#### My response to *Séba Daher`s* comment

🔸 Hi Séba,

Thank you so much for your feedback. It helped me improve my code and practice authentication security very well.

As I mentioned in my previous report, I implemented strong password requirements, including a minimum length and the use of lowercase letters, uppercase letters, and symbols. Now your feedback helped me realise that I had to use all the requirements for make the passwords stronger and improving the security of the authentication system.

Using your feedback to improve my code:

1.I addressed this issue by adding a username uniqueness check. So the second registration request is rejected because the username "Sali awa" already exists, although a different password was entered, the system requires every username to be unique.


This part make the system to reject any attempt to create a second account with an existing username.

```PYTHON

#Check for duplicate usernames to prevent multiple accounts with the same username
for user in self._users:
if user._username==username:
print ("Username already exists.")
return False
 ```

2. Also I have added a validation check to make sure that the password can not be the same as the username.


```PYTHON
check whether the Password does not match the username
if password.lower() == username.lower():
print("Password must not be the same as the username.")
return False

This part to clarify what I added in the test section:

auth_system = AuthenticationSystem()

auth_system.add_user("Sali awa", "UKlondon1993@") # strong password / Successful registration
auth_system.add_user("Sali awa1", "uk1993") # rejected: the password too short,no uppercase and no symbol
auth_system.add_user("Sali awa", "Wrongpassowrd!") # rejected: username already exists
auth_system.add_user("Saliawa1993@", "Saliawa1993@") # rejected because username matching the password

print("Test1:",auth_system.authenticate("Sali awa","UKlondon1993@")) #successful login

print("Test2:",auth_system.authenticate("Sali awa1", "uk1993")) #trying with an unregistered username and a weak password

print("Test3:",auth_system.authenticate("Sali awa","Wrongpassowrd!")) # trying with the correct username but an incorrect password

print("Test4:",auth_system.authenticate("Saliawa1993@", "Saliawa1993@")) #username matching the password
```


#### * Donald Herbert Kofi Appeatsi Duodu`s*comment on my post:

⚫ Hi Sali, really solid thread, and I think the exchange with Séba actually made the code better than the original post did, which is kind of the point of these discussions.

The duplicate username thing Séba picked up on is a good catch. On paper the registration logic looks fine, it's only when you actually try registering the same name twice that it falls apart. That feels like a pattern worth remembering generally, most of these bugs don't jump out when you're reading the code, they show up when you're actively trying to break it. Rehim and Mohan (2024) make a similar point about approaching your own code with an attacker's mindset rather than just reviewing it top to bottom.

I also liked the password not matching the username check. Honestly I didn't think to test for that in my own version, so I'll probably go back and add it now. It's a small thing but it closes off a fairly obvious gap that the length and complexity rules on their own wouldn't catch.

On SonarQube, good shout bringing that in. I'd say tools like that are more of a companion to what's happening in this thread than a replacement for it, they're decent at flagging code smells consistently, but Lenarduzzi et al. (2020) actually found the correlation between SonarQube rules and real bug prevention isn't as strong as you'd expect, so it's still worth having actual people testing and reviewing like you and Séba did here.

One thing I was curious about, did you think about logging failed login attempts anywhere? Not to block them, just so there's some record if an account gets hit with repeated attempts, even after the rate limiting kicks in.

References:   
*Lenarduzzi, V., Lomio, F., Huttunen, H. and Taibi, D. (2020) 'Are SonarQube rules inducing bugs?', in 2020 IEEE 27th International Conference on Software Analysis, Evolution and Reengineering (SANER). IEEE, pp. 501 to 511. Available at: https://doi.org/10.1109/SANER48275.2020.9054821 (Accessed: 10 September 2026).*

Rehim, R. and Mohan, M. (2024) Offensive security using Python: a hands on guide to offensive tactics and threat mitigation using practical strategies. Birmingham: Packt Publishing.

#### My response to *Donald Herbert Kofi Appeatsi Duodu`* comment

🔸 Hi
Thanks for your valuable comment. It raises a very important security point.

I believe that logging failed login attempts would be a useful addition to the authentication system. 

Rehim and Mohan(2024) explain that, logging and monitoring security related events, -including authentication attempts- , can help detected and investigated suspicious activity.


In my code, I can use Python's logging module, for instance:

```python
import logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)
```

Then I can use logger.warning ('This is a warning message') to log failed login attempts and logger.info() to log successful logins, all without storing the user's password.

 This approach will work alongside the failed-attempt counting mechanism: the counter helps prevent repeated attacks, while the log provides a record of what occurred.

I believe this will add another valuable layer of security to the authentication system and reduce the risk of security vulnerabilities.



#### My comment on *Sopheaktra Chea`s*

🔸Hi Sopheaktra,
Thanks for sharing your idea.

You have explained the main security problems clearly, especially the risks of storing passwords in plain text and not having any limit on failed login attempts. Also I like how you used bcrypt and account lockout to make the authentication system more secure.

One thing I found particularly useful was changing the user storage from a list to a dictionary, as this makes finding users faster and more efficient.

The refactoring adds several layers of security while keeping the code organised and easy to understand.




### 💬Unit Reflection

This week focused on secure coding practices in object-oriented programming, showing me how design choices directly affect the safety of a system. I refactored an insecure Python authentication system that had several vulnerabilities.

The main issue was that passwords were stored in plaintext, so anyone accessing the database could read them. I fixed this by hashing passwords with bcrypt and a salt, and using bcrypt.checkpw to compare them instead of matching plain strings. I also added input validation to reject empty entries, enforced a strong password policy (minimum length, uppercase, lowercase, and symbol), and used encapsulation to protect sensitive data. Finally, I added rate limiting to block repeated failed logins and prevent brute-force attacks. Baker (2022) helped inform the password rules I applied.

At first I underestimated how many risks a simple login system can carry, but working through each fix showed me how small design decisions strengthen security. I also saw links to earlier units, as encapsulation and clean structure made the secure version easier to maintain.

Overall, this unit taught me to think about security from the start rather than adding it later. These skills will be valuable when building reliable, secure applications in future projects.

*Baker, M. (2022) Secure Web Application Development: A Hands-On Guide with Python and Django. 1st edn. Berkeley, CA: Apress.*

### Dr.Nawaz Khan comment`s on my initial post:
Hi Sali,
Great examples of refactoring to tackle SQL injection. I agree with the steps, I am just wondering if you can use a tool here for early code smelling and for refactoring suggestions?   

### my replying on Dr.Nawaz cooment`s:   

Hi Dr Nawaz,
Thanks for your comment , it gave me the opportunity to research the tool more widely.

Based on Lenarduzzi et al. (2020), I used SonarQube for IDE in VS Code, as their study shows that it is capable of detecting code quality issues, even though not all of its rules are equally effective at preventing bugs. Unlike ReSharper, which is designed primarily for Visual Studio, SonarQube for IDE integrates directly with VS Code, making it a more suitable choice for my development environment.    

*Reference:*     

*Lenarduzzi, V., Lomio, F., Huttunen, H. and Taibi, D. (2020) 'Are SonarQube rules inducing bugs?', in 2020 IEEE 27th International Conference on Software Analysis, Evolution and Reengineering (SANER). IEEE, pp. 501–511. Available at: https://doi.org/10.1109/SANER48275.2020.9054821 (Accessed: 10 September 2026).*     



After reading the research paper *Are SonarQube rules inducing bugs?*, I installed SonarQube and found that it is a good tool for detecting code smells. It did report some errors in my code. Howerver, it is not 100% accurate, so I could not fix these errors because they are logically correct, because I wanted to validate the usernsame and the password.

![](/assets/Screenshot2026-09-11Toolsmellcode.png)


![](/assets/SonarQubeUNIT7.png)   




![](/assets/imprtbcrypt.png)
